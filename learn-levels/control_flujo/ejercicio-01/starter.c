#include <stdio.h>

/*
 * MISION 09: CONTROL DE REFRIGERACION DE CPU
 * ------------------------------------------
 * Si temp > 30.0 -> printf("Temp: %.2f C -> Alerta: Ventilador ON\n", temp);
 * De lo contrario -> printf("Temp: %.2f C -> Normal: Ventilador OFF\n", temp);
 */

void evaluar_temperatura(float temp) {
    // TODO: Implementa el control if-else

}

int main(void) {
    evaluar_temperatura(32.5);
    evaluar_temperatura(22.0);
    return 0;
}
