#include <stdio.h>

/*
 * MISION 33: MUTACION DIRECTA DE VARIABLE EN MEMORIA
 * --------------------------------------------------
 * Modifica el valor de valor_sensor a traves de su puntero:
 *   1. Imprime: printf("Valor Inicial: %d\n", *ptr);
 *   2. Escribe *ptr = 100;
 *   3. Imprime: printf("Valor Modificado: %d\n", valor_sensor);
 */

int main(void) {
    int valor_sensor = 42;
    int *ptr = &valor_sensor;

    // TODO: Imprime inicial, modifica y vuelve a imprimir


    return 0;
}
