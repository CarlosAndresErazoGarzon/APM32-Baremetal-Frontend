#include <stdio.h>

/*
 * MISION 13: ACUMULADOR DE MUESTRAS EN VENTANA
 * --------------------------------------------
 * Itera 5 pasos (paso = 1..5) sumando muestras impares (1, 3, 5, 7, 9).
 * En cada paso imprime: printf("Muestra %d: %d\n", paso, impar);
 * Al final imprime: printf("Suma Total: %d\n", suma_total);
 */

int main(void) {
    int suma_total = 0;
    int impar = 1;

    // TODO: Bucle for de 5 pasos


    return 0;
}
