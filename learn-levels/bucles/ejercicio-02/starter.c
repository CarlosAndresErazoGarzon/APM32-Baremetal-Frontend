#include <stdio.h>

/*
 * MISION 14: POLLING DE CARGA DE CONDENSADOR
 * ------------------------------------------
 * Monitorea el voltaje en pasos de 0.75V mientras sea < 2.99V:
 *   - En cada ciclo imprime: printf("Paso %d: %.2f V\n", paso, voltaje);
 *   - Al alcanzar la estabilidad imprime: printf("Voltaje Estable Alcanzado!\n");
 */

int main(void) {
    float voltaje = 0.0;
    int paso = 1;

    // TODO: Bucle while para monitoreo de carga


    return 0;
}
