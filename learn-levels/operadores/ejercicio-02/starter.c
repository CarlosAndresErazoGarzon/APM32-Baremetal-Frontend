#include <stdio.h>
#include <stdbool.h>

/*
 * MISION 06: ENCLAVAMIENTO DE SEGURIDAD AMBIENTAL
 * -----------------------------------------------
 * Valida las condiciones seguras de operacion:
 *   - temp_ok = (temp >= 18.0) && (temp <= 30.0)
 *   - hum_ok = (hum >= 30.0) && (hum <= 70.0)
 *   - sistema_seguro = sensor_listo && temp_ok && hum_ok
 * 
 * Salida esperada (%d):
 * Temp OK: 1
 * Humedad OK: 1
 * Sistema Seguro: 1
 */

int main(void) {
    float temp = 24.5;
    float hum = 45.0;
    bool sensor_listo = true;

    // TODO: Evalua los tres enclavamientos booleanos


    // TODO: Imprime los resultados


    return 0;
}
